<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>paas-cse</artifactId>
        <groupId>io.servicecomb</groupId>
        <version>0.1.0-SNAPSHOT</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>integration-tests</artifactId>

    <dependencies>
        <dependency>
            <groupId>io.servicecomb</groupId>
            <artifactId>crossapp-client</artifactId>
            <version>${servicecomb.version}</version>
        </dependency>
    </dependencies>

    <profiles>
        <profile>
            <id>jacoco</id>
            <activation>
                <activeByDefault>false</activeByDefault>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.jacoco</groupId>
                        <artifactId>jacoco-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <phase>verify</phase>
                                <goals>
                                    <goal>report-aggregate</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <profile>
            <id>docker</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>io.fabric8</groupId>
                        <artifactId>docker-maven-plugin</artifactId>
                        <configuration>
                            <images>
                                <image>
                                    <name>seanyinx/sc</name>
                                    <alias>service-center</alias>
                                    <run>
                                        <wait>
                                            <log>listen on server</log>
                                            <time>60000</time>
                                        </wait>
                                        <ports>
                                            <port>service.center.port:9980</port>
                                        </ports>
                                    </run>
                                </image>
                                <image>
                                    <name>crossapp-server:${project.version}</name>
                                    <alias>crossapp-server</alias>
                                    <run>
                                        <env>
                                            <JAVA_OPTS>
                                                -Dcse.rest.address=0.0.0.0:8080 -Dcse.service.registry.address=http://sc.servicecomb.io:9980 -Dcse.service.publishAddress=${docker.hostname}
                                            </JAVA_OPTS>
                                            <JAR_PATH>/maven/crossapp-server-${project.version}.jar</JAR_PATH>
                                        </env>
                                        <links>
                                            <link>service-center:sc.servicecomb.io</link>
                                        </links>
                                        <wait>
                                            <log>Register microservice instance success</log>
                                            <time>120000</time>
                                        </wait>
                                        <ports>
                                            <port>8080:8080</port>
                                        </ports>
                                        <dependsOn>
                                            <dependsOn>service-center</dependsOn>
                                        </dependsOn>
                                    </run>
                                </image>
                            </images>
                        </configuration>
                    </plugin>
                    <plugin>
                        <groupId>org.codehaus.gmaven</groupId>
                        <artifactId>gmaven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>add-default-properties</id>
                                <phase>initialize</phase>
                                <goals>
                                    <goal>execute</goal>
                                </goals>
                                <configuration>
                                    <source>
                                        project.properties.setProperty('docker.hostname', 'localhost')
                                        log.info("Docker hostname is " + project.properties['docker.hostname'])
                                    </source>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-failsafe-plugin</artifactId>
                        <version>2.19.1</version>
                        <configuration>
                            <systemPropertyVariables>
                                <cse.service.registry.address>http://${docker.hostname}:${service.center.port}</cse.service.registry.address>
                            </systemPropertyVariables>
                            <argLine>${jacoco.failsafe.argLine}</argLine>
                        </configuration>
                        <executions>
                            <execution>
                                <goals>
                                    <goal>integration-test</goal>
                                    <goal>verify</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>docker-machine</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.codehaus.gmaven</groupId>
                        <artifactId>gmaven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>add-dynamic-properties</id>
                                <phase>prepare-package</phase>
                                <goals>
                                    <goal>execute</goal>
                                </goals>
                                <configuration>
                                    <source>
                                        def process = "docker-machine ip default".execute()
                                        process.waitFor()
                                        project.properties.setProperty('docker.hostname', process.in.text.trim())

                                        log.info("Docker hostname is " + project.properties['docker.hostname'])
                                    </source>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

</project>