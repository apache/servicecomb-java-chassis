## ---------------------------------------------------------------------------
## Licensed to the Apache Software Foundation (ASF) under one or more
## contributor license agreements.  See the NOTICE file distributed with
## this work for additional information regarding copyright ownership.
## The ASF licenses this file to You under the Apache License, Version 2.0
## (the "License"); you may not use this file except in compliance with
## the License.  You may obtain a copy of the License at
##
##      http://www.apache.org/licenses/LICENSE-2.0
##
## Unless required by applicable law or agreed to in writing, software
## distributed under the License is distributed on an "AS IS" BASIS,
## WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
## See the License for the specific language governing permissions and
## limitations under the License.
## ---------------------------------------------------------------------------
#这个契约是服务端 SchemaId= codeFirstCompute 使用的契约,同时你也可以把这个契约删掉,这样程序会自动生成隐式契约,还可以正常运行
# 使用隐式契约的时候,可以注意 启动provider的 时候,命令行的输出.它会把自动生成的隐式契约打印出来.
#内容和下面的一模一样
swagger: "2.0"
info:
  version: "1.0.0"
  title: "swagger definition for org.apache.servicecomb.samples.pojo.provider.CodeFirstComputeImpl"
  x-java-interface: "cse.gen.pojo_sample.hello.codeFirstCompute.CodeFirstComputeImplIntf"
basePath: "/CodeFirstComputeImpl"
consumes:
- "application/json"
produces:
- "application/json"
paths:
  /add:
    post:
      operationId: "add"
      parameters:
      - in: "body"
        name: "addBody"
        required: false
        schema:
          $ref: "#/definitions/addBody"
      responses:
        200:
          description: "response of 200"
          schema:
            type: "integer"
            format: "int32"
  /sub:
    post:
      operationId: "sub"
      parameters:
      - in: "body"
        name: "subBody"
        required: false
        schema:
          $ref: "#/definitions/subBody"
      responses:
        200:
          description: "response of 200"
          schema:
            type: "integer"
            format: "int32"
  /divide:
    post:
      operationId: "divide"
      parameters:
      - in: "body"
        name: "divideBody"
        required: false
        schema:
          $ref: "#/definitions/divideBody"
      responses:
        200:
          description: "response of 200"
          schema:
            type: "integer"
            format: "int32"
  /multi:
    post:
      operationId: "multi"
      parameters:
      - in: "body"
        name: "multiBody"
        required: false
        schema:
          $ref: "#/definitions/multiBody"
      responses:
        200:
          description: "response of 200"
          schema:
            type: "integer"
            format: "int32"
definitions:
  addBody:
    type: "object"
    properties:
      a:
        type: "integer"
        format: "int32"
      b:
        type: "integer"
        format: "int32"
    x-java-class: "cse.gen.pojo_sample.hello.codeFirstCompute.addBody"
  divideBody:
    type: "object"
    properties:
      a:
        type: "integer"
        format: "int32"
      b:
        type: "integer"
        format: "int32"
    x-java-class: "cse.gen.pojo_sample.hello.codeFirstCompute.divideBody"
  multiBody:
    type: "object"
    properties:
      a:
        type: "integer"
        format: "int32"
      b:
        type: "integer"
        format: "int32"
    x-java-class: "cse.gen.pojo_sample.hello.codeFirstCompute.multiBody"
  subBody:
    type: "object"
    properties:
      a:
        type: "integer"
        format: "int32"
      b:
        type: "integer"
        format: "int32"
    x-java-class: "cse.gen.pojo_sample.hello.codeFirstCompute.subBody"
